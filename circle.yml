machine:
  xcode:
    version: "9.0"
  environment:
    LANG: en_US.UTF-8

dependencies:
  override:
    - swift package update
  cache_directories:
    - Packages

test:
  override:
    - swift build